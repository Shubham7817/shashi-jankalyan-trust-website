"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.paintBoxShadow = void 0;
const color_utilities_1 = require("../../css/types/color-utilities");
const constants_1 = require("../../core/constants");
const bound_curves_1 = require("../bound-curves");
const box_sizing_1 = require("../box-sizing");
const path_1 = require("../path");
const canvas_path_1 = require("./canvas-path");
const filter_surface_1 = require("./filter-surface");
const surface_bounds_1 = require("./surface-bounds");
const box_shadow_geometry_1 = require("./box-shadow-geometry");
const box_shadow_transform_1 = require("./box-shadow-transform");
// Separate closed subpaths plus even-odd filling avoid reversing only the order
// of corner Beziers, which does not reverse the curves themselves.
const complement = (ctx, rect, hole) => {
    ctx.beginPath();
    ctx.rect(rect.left, rect.top, rect.width, rect.height);
    if (hole.length) {
        (0, canvas_path_1.formatCanvasPath)(ctx, hole);
        ctx.closePath();
    }
};
const insetHole = (paint, shadow) => {
    const spread = shadow.spread.number;
    const box = (0, box_sizing_1.paddingBox)(paint.container);
    if (box.width - 2 * spread <= 0 || box.height - 2 * spread <= 0)
        return [];
    return (0, box_shadow_geometry_1.spreadShadowPath)((0, bound_curves_1.calculatePaddingBoxPath)(paint.curves), -spread);
};
// Rasterize in local coordinates, blur once, and then let the destination CTM
// transform the complete shadow. This also preserves anisotropic/sheared blur.
const paintShadowSurface = async (ctx, paint, shadow, options, budget, space) => {
    const padding = (0, box_sizing_1.paddingBox)(paint.container);
    const blur = shadow.blur.number / 2;
    const margin = Math.ceil(3 * blur) + 2;
    const spread = shadow.spread.number;
    const area = shadow.inset
        ? padding
        : paint.container.bounds.add(shadow.offsetX.number - spread, shadow.offsetY.number - spread, 2 * spread, 2 * spread);
    const bounds = (0, surface_bounds_1.cropSurface)(area.add(-margin, -margin, 2 * margin, 2 * margin), space.viewport, margin, space.scale);
    if (!bounds.width || !bounds.height)
        return true;
    const width = Math.ceil(bounds.width * space.scale);
    const height = Math.ceil(bounds.height * space.scale);
    const reserved = (0, surface_bounds_1.reserveSurface)(budget, width, height);
    if (!reserved)
        return false;
    let source;
    let filtered;
    try {
        source = ctx.canvas.ownerDocument.createElement('canvas');
        source.width = width;
        source.height = height;
        const sourceCtx = source.getContext('2d');
        if (!sourceCtx)
            return false;
        sourceCtx.scale(space.scale, space.scale);
        sourceCtx.translate(-bounds.left, -bounds.top);
        const silhouette = shadow.inset
            ? insetHole(paint, shadow)
            : (0, box_shadow_geometry_1.spreadShadowPath)((0, bound_curves_1.calculateBorderBoxPath)(paint.curves), spread);
        const path = (0, path_1.transformPath)(silhouette, shadow.offsetX.number, shadow.offsetY.number, 0, 0);
        if (shadow.inset)
            complement(sourceCtx, bounds, path);
        else
            (0, canvas_path_1.createCanvasPath)(sourceCtx, path);
        sourceCtx.fillStyle = (0, color_utilities_1.asString)(shadow.color);
        sourceCtx.fill(shadow.inset ? 'evenodd' : 'nonzero');
        filtered = await (0, filter_surface_1.renderFilterSurface)(source, { blur }, 1, space.scale, options.signal);
        ctx.save();
        try {
            if (shadow.inset) {
                (0, canvas_path_1.createCanvasPath)(ctx, (0, bound_curves_1.calculatePaddingBoxPath)(paint.curves));
                ctx.clip();
            }
            else {
                complement(ctx, space.viewport, (0, bound_curves_1.calculateBorderBoxPath)(paint.curves));
                ctx.clip('evenodd');
            }
            ctx.drawImage(filtered, bounds.left, bounds.top, filtered.width / space.scale, filtered.height / space.scale);
        }
        finally {
            ctx.restore();
        }
        return true;
    }
    catch (error) {
        if (!(error instanceof filter_surface_1.FilterSurfaceError))
            throw error;
        return false;
    }
    finally {
        if (filtered && filtered !== source)
            (0, filter_surface_1.releaseSurface)(filtered);
        if (source)
            (0, filter_surface_1.releaseSurface)(source);
        budget.pixels -= reserved;
    }
};
const paintBoxShadow = async (ctx, paint, shadow, options, budget) => {
    if (options.signal?.aborted)
        throw new DOMException('The operation was aborted.', 'AbortError');
    if ((0, color_utilities_1.isTransparent)(shadow.color))
        return;
    const spread = shadow.spread.number;
    if (!shadow.inset &&
        (paint.container.bounds.width + 2 * spread <= 0 || paint.container.bounds.height + 2 * spread <= 0))
        return;
    // getTransform includes capture scale, CSS transforms and their origins.
    // Without it the CTM is unknown: approximate it with the capture transform
    // and only ever move the mask relative to the real CTM (see below).
    const knownTransform = typeof ctx.getTransform === 'function';
    const matrix = knownTransform
        ? ctx.getTransform()
        : {
            a: options.scale,
            b: 0,
            c: 0,
            d: options.scale,
            e: -options.x * options.scale,
            f: -options.y * options.scale
        };
    const space = (0, box_shadow_transform_1.shadowSpace)(matrix, ctx.canvas.width, ctx.canvas.height);
    // Singular transforms have zero painted area; never attempt to invert them.
    if (!space)
        return;
    const { viewport } = space;
    if (shadow.blur.number > 0 &&
        (shadow.inset || !space.uniform) &&
        (await paintShadowSurface(ctx, paint, shadow, options, budget, space)))
        return;
    const padding = (0, box_sizing_1.paddingBox)(paint.container);
    ctx.save();
    try {
        if (shadow.inset && shadow.blur.number === 0) {
            (0, canvas_path_1.createCanvasPath)(ctx, (0, bound_curves_1.calculatePaddingBoxPath)(paint.curves));
            ctx.clip();
            complement(ctx, padding, (0, path_1.transformPath)(insetHole(paint, shadow), shadow.offsetX.number, shadow.offsetY.number, 0, 0));
            ctx.fillStyle = (0, color_utilities_1.asString)(shadow.color);
            ctx.fill('evenodd');
            return;
        }
        const margin = Math.ceil(shadow.blur.number * 2 +
            Math.abs(spread) +
            Math.abs(shadow.offsetX.number) +
            Math.abs(shadow.offsetY.number)) + 2;
        // Displace in OUTPUT pixels, not along a CSS axis. A rotated or reflected
        // local x axis need not point off the left edge of the output bitmap.
        const sourceBounds = shadow.inset
            ? padding.add(-margin, -margin, 2 * margin, 2 * margin)
            : paint.container.bounds.add(-spread, -spread, 2 * spread, 2 * spread);
        const { a, b, c, d, e, f } = matrix;
        const right = sourceBounds.left + sourceBounds.width;
        const bottom = sourceBounds.top + sourceBounds.height;
        const maxX = Math.max(a * sourceBounds.left + c * sourceBounds.top + e, a * right + c * sourceBounds.top + e, a * right + c * bottom + e, a * sourceBounds.left + c * bottom + e);
        const displacement = Math.max(constants_1.SHADOW_MASK_OFFSET, maxX + margin * space.scale + 2);
        if (shadow.inset) {
            (0, canvas_path_1.createCanvasPath)(ctx, (0, bound_curves_1.calculatePaddingBoxPath)(paint.curves));
            ctx.clip();
        }
        else {
            complement(ctx, viewport, (0, bound_curves_1.calculateBorderBoxPath)(paint.curves));
            ctx.clip('evenodd');
        }
        // Current paths retain their device coordinates across transform changes.
        // setTransform with an approximate matrix would discard CSS transforms
        // on the real CTM, so the legacy path translates relative to it instead.
        if (knownTransform)
            ctx.setTransform(a, b, c, d, e - displacement, f);
        else
            ctx.translate(-displacement / a, 0);
        if (shadow.inset)
            complement(ctx, sourceBounds, insetHole(paint, shadow));
        else
            (0, canvas_path_1.createCanvasPath)(ctx, (0, box_shadow_geometry_1.spreadShadowPath)((0, bound_curves_1.calculateBorderBoxPath)(paint.curves), spread));
        if (knownTransform)
            ctx.setTransform(a, b, c, d, e, f);
        else
            ctx.translate(displacement / a, 0);
        ctx.shadowOffsetX = displacement + a * shadow.offsetX.number + c * shadow.offsetY.number;
        ctx.shadowOffsetY = b * shadow.offsetX.number + d * shadow.offsetY.number;
        // Allocation/filter-failure fallback remains bounded-memory. Its blur is
        // isotropic under nonuniform transforms, but positioning stays correct.
        ctx.shadowBlur = shadow.blur.number * space.scale;
        ctx.shadowColor = (0, color_utilities_1.asString)(shadow.color);
        ctx.fillStyle = 'rgb(0, 0, 0)';
        ctx.fill(shadow.inset ? 'evenodd' : 'nonzero');
    }
    finally {
        ctx.restore();
    }
};
exports.paintBoxShadow = paintBoxShadow;
